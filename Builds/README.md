# Builds

Esta pasta recebe o executável Windows gerado pelo Unity (`ValeDosCristais.exe`) e o ZIP de entrega.
Os binários exportados ficam ignorados pelo Git para evitar arquivos grandes no repositório.
